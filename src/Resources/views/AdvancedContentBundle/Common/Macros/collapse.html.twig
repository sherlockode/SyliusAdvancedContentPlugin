{% macro buildAccordion(data) %}
    {% for item in data %}
        <div class="mb-3">
            <details>
                <summary>{{ item.title|raw }}</summary>
                {{ item.content|raw }}
            </details>
        </div>
    {% endfor %}
{% endmacro %}
